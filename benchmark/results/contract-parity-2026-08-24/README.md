# Native/Web Contract Parity Result

Generated by:

```bash
node Tests/pdf_contract_parity_test.mjs
```

This directory is derived evidence for the ten-entry fixture manifest. It is
not a second source of truth for the contracts.

- `native/` contains serialized bundles emitted by the macOS
  `PDFContractHarness` Swift executable through PDFKit.
- `web/` contains serialized bundles emitted by the browser PDF.js/pdf-lib
  fixture through isolated Chrome.
- `parity-report.json` contains the normalized comparison and first mismatch
  records.

The 2026-08-24 run contains ten fixtures, matching source digests for all
successfully inspected inputs, an expected malformed-input failure in both
lanes, and 75 normalized semantic mismatches. Interpret the mismatch taxonomy
through [`docs/audits/native-web-contract-parity-evidence-2026-08-24.md`](../../../docs/audits/native-web-contract-parity-evidence-2026-08-24.md).

The PDFs themselves are not copied into this result directory. Their canonical
paths, hashes, and provenance remain in [`docs/fixtures/manifest.md`](../../../docs/fixtures/manifest.md).
